export const categoriesSelector = (state) => {
    return state.sidebar.categories
}

export const tagsSelector = (state) => {
    return state.sidebar.tags
}

export const archivesSelector = (state) => {
    return state.sidebar.archives
}